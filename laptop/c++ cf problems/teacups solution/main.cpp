#include <bits/stdc++.h>
#define int long long
#define pb push_back
#define ff first
#define ss second
#define pii pair<int,int>
#define vi vector<int>
#define vii vector<pair<int,int>>
#define print(x) cout << (x) << endln
#define each(a, x) for(auto &&a : x)

using namespace std;
using ll = long long;
using ld = long double;
using u128 = unsigned __int128;
using i128 = __int128;

void solve(){
    int n,s;
    cin >> n >> s;
    pair<pii,int> circles;
    for(int i=0;i<n;i++){
        cin >> circles.ff.ff >> circles.ff.ss >> circles.ss;
    }
    vector<vi> adj(n);

}

signed main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    return 0;
}
